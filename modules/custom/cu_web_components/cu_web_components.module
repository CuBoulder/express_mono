<?php

/**
 * Implements hook_page_alter().
 *
 */
function cu_web_components_page_alter(&$page) {
    drupal_add_js(drupal_get_path('module', 'cu_web_components') .'/ucbCovidStats.bundle.js', 
        array(
            'scope' => 'footer',
            'defer' => true
        )
    );
}