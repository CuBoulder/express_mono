<?php

/**
 * @file
 * Drupal install hooks.
 */

/**
 * Implements hook_install().
 */
function express_site_metrics_install() {
  global $conf;
  $prometheusToken = $conf['express_site_metrics_prometheus'];

  variable_set('express_site_metrics_users', $prometheusToken);
}
